FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV PORT=8000 \
BASE_URL=http://localhost:8000 \
DB_PATH=/app/data.sqlite3
EXPOSE 8000
CMD ["python", "app.py"]
